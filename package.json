{
  "name": "@ludic/ein",
  "version": "1.0.1",
  "description": "Entity Component System",
  "format": "es6",
  "main": "lib/main.js",
  "types": "lib/main.d.ts",
  "scripts": {
    "dev": "tsc -w",
    "build": "rimraf lib && tsc",
    "test": "npm run build && nyc mocha --require ts-node/register -r tsconfig-paths/register -r source-map-support/register --recursive \"./tests/components.spec.ts\"",
    "test:all": "npm run build && nyc mocha --require ts-node/register -r tsconfig-paths/register -r source-map-support/register --recursive \"./tests/**/*.spec.ts\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Ludic/ein.git"
  },
  "devDependencies": {
    "@types/chai": "^4.1.4",
    "@types/mocha": "^5.2.5",
    "@types/node": "^11.9.5",
    "@types/sinon": "^5.0.1",
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "nyc": "^12.0.2",
    "rimraf": "^2.6.3",
    "sinon": "^6.1.4",
    "source-map-support": "^0.5.6",
    "ts-node": "^7.0.0",
    "tsconfig-paths": "^3.5.0",
    "typescript": "^3.2.2"
  }
}
