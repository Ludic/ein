{
  "name": "@ludic/ein",
  "version": "1.0.0",
  "description": "Entity Component System",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Ludic/ein.git"
  },
  "directories": {
    "example": "example"
  },
  "scripts": {
    "dev": "parcel watch --no-autoinstall ./src/index.ts",
    "clean": "rimraf ./dist",
    "build": "parcel build --no-autoinstall ./src/index.ts --no-cache",
    "bnt": "npm run build && npm run test",
    "example": "parcel ./example/index.html --no-autoinstall --no-cache -p 8080",
    "test": "ts-mocha tests/**/*.spec.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.2.9",
    "@types/mocha": "^7.0.1",
    "mocha": "^7.0.1",
    "parcel": "^1.12.4",
    "ts-mocha": "^6.0.0",
    "typescript": "^3.7.5"
  },
  "dependencies": {
    "chai": "^4.2.0"
  }
}
